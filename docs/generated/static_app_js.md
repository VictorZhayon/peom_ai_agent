# static/app.js

This file contains the frontend JavaScript logic for the peom_ai_agent, handling user interactions, poem generation, display, and saving.

## pick

```javascript
function pick(arr) { return arr[Math.floor(Math.random() * arr.length)]; }
```

Selects a random element from the given array.

### Parameters

*   `arr`: The input array.

### Returns

A randomly selected element from the array.
